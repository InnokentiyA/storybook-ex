{/* LoginForm.mdx */}

import { Canvas, Meta } from "@storybook/blocks";

import * as LoginFromStories from "./LoginForm.stories.js";

<Meta of={LoginFromStories} name="Info" />

# Login form

Here we test the three main states of component user interactions. The default state of component before
user interactions, user positive login and user negative login.

### Default Login form

<Canvas of={LoginFromStories.LoginFormDefault} />
### Successful Login
- In this example, the `Meta` component is used to invoke
positive response on the UI.
<Canvas of={LoginFromStories.SuccessfulLogin} />
### Failed Login
- In this example, the `Meta` component is used to invoke
negative response on the UI.
<Canvas of={LoginFromStories.LoginWithEmptyUsername} />
<Canvas of={LoginFromStories.LoginWithEmptyPassword} />
